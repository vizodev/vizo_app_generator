# Register bricks which can be consumed via the Mason CLI.
# https://github.com/felangel/mason
bricks:
  bagel:
    path: bricks/bagel
  bagel_collection_example:
    path: bricks/bagel_collection_example
  get_it:
    path: bricks/get_it
  linter_rules:
    path: bricks/linter_rules
  url_launcher:
    path: bricks/url_launcher
  vizo_app_generator:
    path: bricks/vizo_app_generator
  vizo_text:
    path: bricks/vizo_text

  # Run `mason make hello` to try it out.
  hello: 0.1.0+1
  # widget:
  #   git:
  #     url: https://github.com/felangel/mason.git
  #     path: bricks/widget
